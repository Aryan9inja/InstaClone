import { createSlice } from "@reduxjs/toolkit";

const initialState={
    userId:null,
    userData:null,
}

const userSlice=createSlice({
    name:"user",
    initialState,
    reducers:{
        
    }
})